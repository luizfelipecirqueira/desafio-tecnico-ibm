import React from 'react';
import { ContainerHeader, TextoHeader } from '../../styles/Header';

export const Header = () => {
    return (
        <ContainerHeader>
            <TextoHeader>Projeto Busca de Livros - IBM</TextoHeader>
        </ContainerHeader>
    )
}